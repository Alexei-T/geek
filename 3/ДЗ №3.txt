ЗАДАНИЕ:
1)Создать файл file1 и наполнить его произвольным содержимым. Скопировать его в file2. Создать символическую ссылку file3 на file1. Создать жесткую ссылку file4 на file1.         Посмотреть, какие айноды у файлов. Удалить file1. Что стало с остальными созданными файлами? Попробовать вывести их на экран.
2)Дать созданным файлам другие, произвольные имена. Создать новую символическую ссылку. Переместить ссылки в другую директорию. Создать два произвольных файла. Первому 
присвоить права на чтение, запись для владельца и группы, только на чтение — для всех. Второму присвоить права на чтение, запись — только для владельца. Сделать это в 
численном и символьном виде.
3)Создать пользователя, обладающего возможностью выполнять действия от имени суперпользователя.
4*Создать группу developer и несколько пользователей, входящих в нее. Создать директорию для совместной работы. Сделать так,
чтобы созданные одними пользователями файлы могли изменять другие пользователи этой группы.
5*Создать в директории для совместной работы поддиректорию для обмена файлами, но чтобы удалять файлы могли только их создатели.
6*Создать директорию, в которой есть несколько файлов. Сделать так, чтобы открыть файлы можно было, только зная имя файла, а через ls список файлов посмотреть было нельзя.

РЕШЕНИЕ:
1)touch file1; echo hello > file1
cp file1 file2; ln -s file1 file3
ln file1 file4
ls -li file*
136 -rw-rw-r-- 2 alex alex 6 Nov 27 22:23 file1
142 -rw-rw-r-- 1 alex alex 6 Nov 27 22:26 file2
145 lrwxrwxrwx 1 alex alex 5 Nov 27 22:26 file3 -> file1
136 -rw-rw-r-- 2 alex alex 6 Nov 27 22:23 file4
rm -rf file1
В результате доступны и читаются файлы file2, file4. Файл file3(символьная ссылка) доступен,
но не читается, т.к. файл file1 был удален.

2)mv file2 test2; mv file3 test3; mv file4 test4
ln -s test2 simlink; ln -s test4 simlink2
mkdir dirr; mv simlink* dirr/
touch kek1 kek2
chmod 664 kek1 #(В буквенном виде: chmod u=rw,g=rw,o=r kek1)
chmod 600 kek2 #(В буквенном виде: chmod u=rw,g-rwx,o-rwx kek2)

3)Дать привилегии sudo в CentOS - самый простой способ, это добавить пользователя в группу "wheel"
useradd testuser
usermod -aG wheel testuser

4* groupadd developer
useradd user1 -G developer
useradd user2 -G developer
mkdir dir1
chown root:developer dir1/
chmod 2770 dir1/
После установки SGID на папку, все новые файлы и папки внутри "dir1/" будут иметь группу "developer", соответсвенно user1 
и user2 смогут редактировать файлы друг друга  

5* mkdir /dir1/newdir
chmod +t /dir1/newdir/
После установки sticky-бит файлы внутри "/dir1/newdir/" могут быть удалены только их владельцами

6* mkdir dir22; touch dir22/file{1..3}
chmod ugo-r dir22/
cat dir22/file1 (так же file2,file3) покажет содержимое файлов

